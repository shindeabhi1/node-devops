/*
const app = require('../../src/app');

describe('App Unit Tests', () => {
  test('Express app should be defined', () => {
    expect(app).toBeDefined();
  });
});
*/

test('basic unit test works', () => {
  expect(1 + 1).toBe(2);
});
``
